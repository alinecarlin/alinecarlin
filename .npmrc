# Keep workspace installs forgiving across packages that rely on peer dependency declarations.
auto-install-peers=true
# Avoid failing installation on peer mismatches while the monorepo is still evolving.
strict-peer-dependencies=false